apiVersion: v1
kind: ConfigMap
metadata:
    name: {{ .Values.crasherCMName }}
data:
    conf-file.yaml: |
      name: "Hello World"
      other-thing: foo
      some-things:
        - thing1
        - thing2
